/* Diagnostic-only, bounded, single recording per process. All data mutations
 * use g_cap_cs; only XR polls keys, reads staging or ends the recording.
 * GPU copies happen while waited XR images are owned, inside the capture
 * transaction. Later reads touch private staging only, never XR images.
 * File IO happens on a writer after recording stops, never in Present/Map.
 */

























































































































































































































































































































int dg_xr_measure_control(char*d,unsigned c,unsigned*m){(void)d;(void)c;(void)m;return 0;}
unsigned dg_xr_measure_trace_request(void){return 0;}
#define sp_capture(back,store,eye,id) ((void)0)
void dg_xr_measure_present(const DG_STEREO_PRESENT *p){(void)p;}
#define sm_producer(kind,eye,raw,fov,meta,id) ((void)0)
#define sm_copy(eye,stereo,dst) ((void)0)
#define sm_tick() ((void)0)
#define sm_submit(fei) ((void)0)
#define sm_init() ((void)0)
#define sm_teardown() ((void)0)
#define sm_shutdown() ((void)0)

